# Synth Zed backlog

Active backlog for this fork, using the [Local Markdown provider](providers/local-markdown.md).
GitHub Issues and Discussions are disabled on `synthet/zed`, so this file is the system of record.

Statuses: `Backlog`, `Ready`, `Claimed`, `In Progress`, `Blocked`, `Review`, `Done`.
Claim by setting `Owner` and `Status`; reference the ID in the PR body (`Refs L-001`).
Move completed items to `archive.md` if this list grows too large.

Scope note: this list is **fork-owned work only**. The 281 inherited upstream `TODO`/`FIXME`/`HACK`
markers under `crates/` are excluded by decision — they belong to upstream Zed.

## Items

| ID | Status | Priority | Owner | Title | Notes |
|----|--------|----------|-------|-------|-------|
| L-001 | Done | p1 | - | Add missing LICENSE symlink to `msvc_spectre_libs_stub` | `script/check-licenses` requires a `LICENSE-GPL`/`LICENSE-APACHE` symlink in every dir with a `Cargo.toml`; `tooling/msvc_spectre_libs_stub` was the only one missing it, so CI (`run_tests.yml:796`) would fail on Linux. Regression from `2a60ab4b75`. Fixed by staging `LICENSE-APACHE` with mode `120000` (blob matches the 41 depth-2 peers). |
| L-002 | Done | p2 | - | Commit and verify the Synthet theme bundle | 29 theme JSONs under `assets/themes/synthet/`, `crates/theme_settings/tests/synthet_default_themes.rs`, and an `assets` dev-dep in `crates/theme_settings/Cargo.toml` + `Cargo.lock`. Wiring is sound: `crates/assets` already embeds `themes/**/*` and `load_bundled_themes` walks recursively. L-003 and L-004 are decided and applied; catalog is now 40 themes (11 existing + 8 popular-dark + 21 warp-defaults). Themes use plain display names per `shared_catalog_themes_have_plain_bundled_names`; 4 names collide (Dracula, Gruvbox Dark, Gruvbox Light, Solarized Dark) so 40 variants present 36 distinct names, last-loaded wins - see PROVENANCE.md. Verified: `cargo test -p theme_settings --test synthet_default_themes` passes, 1 passed 0 failed. `cargo nextest` is not installed on this machine; use `cargo test` here. |
| L-003 | Done | p1 | - | Decide: AGPL-derived Warp theme adaptations vs. anti-AGPL policy | `script/check-licenses` `check_no_agpl_license_file()` states first-party code must use `LICENSE-GPL` or `LICENSE-APACHE`. The pending bundle adds `assets/themes/synthet/LICENSE-AGPL-3.0`, and `warp-defaults/` are adaptations of `AGPL-3.0-only` Warp source. It evades the guard only because the guard globs `*/LICENSE-AGPL` and the file is suffixed `-3.0` — coincidence, not compliance. Resolved: Warp publishes its bundled themes at `warpdotdev/themes` under Apache-2.0. 18 of 21 kept themes were verified against that source at commit `82e51dcf9b` - all 16 ANSI colors and foreground identical; 6 gradient backgrounds flattened to the midpoint and `dark` lifted from `#000000` to `#050505`, both documented. `adeberry`, `phenomenon`, `solar-flare` have no counterpart across all 337 published themes, so they keep AGPL provenance by owner decision; `LICENSE-AGPL-3.0` now scopes to those 3 files instead of 23. |
| L-004 | Done | p1 | - | Decide: redistributing the Monokai Pro theme | `assets/themes/synthet/PROVENANCE.md` states the public Monokai Pro repo describes a proprietary commercial theme and publishes no license. Resolved: dropped. No published license means no redistribution grant. File and test entry removed. |
| L-005 | Done | p3 | - | Fold Synthet theme attribution into `assets/themes/LICENSES` | Existing bundled themes aggregate attribution in `assets/themes/LICENSES`; the Synthet bundle introduced a parallel `PROVENANCE.md`. Fold whatever survives L-003/L-004 into the established file. Done: three license-grouped sections appended after verifying every theme against its real upstream; `PROVENANCE.md` narrowed to the engineering record. |
| L-006 | Done | p3 | - | Stop embedding non-theme files in the binary | `#[include = "themes/**/*"]` in `crates/assets/src/assets.rs` embeds `PROVENANCE.md` and `LICENSE-AGPL-3.0`. Harmless at runtime (`load_bundled_themes` filters `.json`) but dead bytes. Add an `#[exclude]` or move the docs out of `assets/`. Done: three `#[exclude]` attributes plus a test asserting only `.json` is embedded under `themes/`. |
| L-007 | Done | p3 | - | Document the Synthet theme catalog | No mention in `docs/architecture/fork-deltas.md`, and no `docs/log.md` entry — the repo treats that log as an append-only record of every wiki/asset change. Done: themes section in `fork-deltas.md`, attribution row in `CANONICAL_SOURCES.md`, log line. |
| L-008 | Done | p1 | - | Sync local `main` with `origin/main` | Local was 3 commits behind: `36c8be5338` (merge of `zed-industries/main`), `56b1e79a55`, `1e3d8b5a66`. Fast-forwarded; now level with origin. |
| L-009 | Done | p2 | - | Remove stale cargo-check scratch logs | Three untracked Aug-14 logs sat in the repo root, unmatched by `.gitignore`. Deleted, and `cargo-check-*.log` added to `.gitignore`. The `msvc_spectre_libs` failure they recorded was already fixed by the `Cargo.toml:981` stub patch. |
| L-010 | Done | p2 | - | Fix the backlog provider contradiction in `CLAUDE.md` | It named GitHub Issues as the sole work source (disabled on the repo) and forbade `.agent/backlog/items.md` (the provider now in use). Rewritten to point at this file and the Local Markdown provider. Also corrected in `docs/project/00-backlog-workflow.md`, `docs/CANONICAL_SOURCES.md`, and `docs/architecture/fork-deltas.md`. |
| L-011 | Ready | p3 | - | Update the `synthet/zed` GitHub repo description | Still upstream Zed's: "high-performance, multiplayer code editor from the creators of Atom and Tree-sitter". Wrong branding, and "multiplayer" contradicts the fork's local-first stance. Repo-settings change, owner-only. |
| L-012 | Backlog | p3 | - | Prune inherited upstream GitHub workflows | All 46 files in `.github/workflows/` reference `zed-industries`, collab, or org secrets this fork does not hold — `deploy_collab`, `bump_collab_staging`, `guild_*`, `triage_*`, `community_*`, `slack_notify_*`, duplicate-bot. Dead weight or noisy failures, and they contradict "strip hosted commercial surfaces". Plausible keepers: `run_tests`, `release*`, `nix_build`, `run_bundling`. |
| L-013 | Done | p3 | - | Reconcile telemetry/collab crates with the documented fork deltas | Premise was wrong: the sinks are severed in code, not merely documented. Verified `flush_events_inner` drains to a local log with no HTTP (`crates/client/src/telemetry.rs:639`); minidump / Sentry / build-timing uploads stubbed (`crates/zed/src/reliability.rs:36,178,183`); `CollabPanel` omitted from `initialize_panels` (`crates/zed/src/zed.rs:784`); collaboration settings page hidden (`crates/settings_ui/src/page_data.rs:79`); `CloudLanguageModelProvider` unregistered (`crates/language_models/src/language_models.rs:220`); `diagnostics`/`metrics` default false. No startup autoconnect — the sole `client.connect` outside `crates/client` (`crates/zed/src/main.rs:1313`) is gated behind an explicit `zed://channel/...` URL. `crates/collab` is depended on by nothing but itself and carries no billing/Stripe code. Present-but-unwired **is** the intended end state, already stated correctly in `fork-deltas.md:22,50`; removal is not outstanding. Fixed the one overstatement (`CLAUDE.md:29`, `removes` -> `unwires` for the collab clause) and hid the orphaned `collaboration_panel_section()` that still offered dock settings for a panel this fork never loads. Spun out L-015 and L-016. |
| L-014 | Backlog | p3 | - | Re-verify dead code in fork-touched files | An Aug-14 check flagged `crates/language_models/src/provider/anthropic/telemetry.rs:32,42` (`as_str` never used) and an unused `user_store` param in `crates/onboarding` `render_ai_section` — plausibly orphaned by the commercial-surface strip. Evidence predates several commits; re-run a check before acting. 2026-08-18: one fresh confirmed hit — `crates/zed/src/reliability.rs:178` `upload_previous_minidumps` is dead because the fork stubbed its caller at line 36; unlike the equivalent stub at `crates/settings_ui/src/page_data.rs:8084` it never got an `#[allow(dead_code)]`. |
| L-015 | Backlog | p3 | - | `server_url` still defaults to `https://zed.dev` | `assets/settings/default.json:2690` points the collab/rpc client at Zed's infrastructure. Not a live leak today — nothing autoconnects (see L-013) — but it contradicts the local-first default posture and is the one remaining zed.dev endpoint baked into shipped settings. Decide: leave as-is for opt-in self-host, blank it, or point at a placeholder. |
| L-016 | Done | p2 | - | `CLAUDE.md` and `AGENTS.md` are committed with symlink mode | Both are recorded in the index as mode `120000` (symlink) while their blobs hold the full document text (5246 and 5538 bytes). Only these 2 of 265 symlink-mode entries are affected; the rest are legitimate `LICENSE-*` links. Harmless on Windows because `core.symlinks=false` materializes them as regular files, but a POSIX clone would try to create a symlink whose target is the entire document. Found incidentally during L-013. Fixed with `git update-index --cacheinfo 100644,<sha>,<path>`, which re-records the mode while reusing the identical blob, so no content or line-ending change was possible: `git diff --cached` showed `mode change 120000 => 100644` with 0 insertions and 0 deletions. Rescanned afterwards — all 263 remaining symlink entries hold single-line relative paths (`GEMINI.md -> .rules`, `crates/*/LICENSE-GPL -> ../../LICENSE-GPL`) and are genuine. |
